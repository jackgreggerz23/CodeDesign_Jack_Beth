
        
    var c = document.getElementById("static");
    var ctx = c.getContext("2d");

    //red circle
 
    ctx.beginPath();//Begins the path.
    ctx.arc(180,190,100,1.6*Math.PI,1.4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour the path will be.
    ctx.fill();//Fills the arc.
    ctx.lineWidth = 5;//Chooses the line width of the storke.
    ctx.stroke();//Strokes the outline.
    ctx.closePath();//Closes the path.
    
    //red circle end
    
    
   
    
    
    //Half circles (stomach) x2
    
    ctx.beginPath();//Begins the path.
    ctx.arc(180,350,100,1.3*Math.PI,1.7*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#d9b38c");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(180,188,100,0.3*Math.PI,0.7*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#d9b38c");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
   //Half circles (stomach) end
    
    
    
    
    
    //Dark red circle 1 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(130,230,15,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(112.2,230,15,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    //Dark red circle end
    
    
    
    
    //Dark red circle 2 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(160,220,20,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(135.2,220,20,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    //Dark red circles end
    
    
    
    
    
    
    //Dark red circle 3 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(190,210,25,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(160.2,210,25,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    //Dark red circles end
    
    
    
    
    //Dark red circle 4 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(225,210,30,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(188.5,210,30,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
   
    //Dark red circles end
    
    //Dark red circle 5
    
    ctx.beginPath();//Begins the path.
    ctx.arc(260,210,30,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(224,210,30,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    //Dark red circles end
    
   
    //Dark red circle 6 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(277,215,20,0.7*Math.PI,1.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(253,215,20,1.7*Math.PI,2.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#660000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.closePath();//Closes the path.
    
    //Dark red circles end
    
    
    
    
    
    //eye outlines x2
    ctx.beginPath();//Begins the path.
    ctx.arc(200,190,15,0.7*Math.PI,0.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#ffffff");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 3;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
    
    
    ctx.beginPath();//Begins the path.
    ctx.arc(230,190,15,0.7*Math.PI,0.3*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#ffffff");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 3;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
    
    //eye outlines end
    
    
    
    
    
    
    
    //eye bags x2
    ctx.beginPath();//Begins the path.
    ctx.arc(200,227,25,1.3*Math.PI,1.7*Math.PI);//Points of the arc.
    ctx.lineWidth=4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(231,227,25,1.3*Math.PI,1.7*Math.PI);//Points of the arc.
    ctx.lineWidth=4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
    
    //eyebags end
    
    
    
    
    
    //eyeballs x2
    ctx.beginPath();//Begins the path.
    ctx.arc(202,194,5,0*Math.PI,2*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.  
        
    ctx.beginPath();//Begins the path.
    ctx.arc(232,194,5,0*Math.PI,2*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#000");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
    
    //eyeballs end
   
        
        
        
        
        
        
        
    
    //Hair right
    ctx.beginPath();//Begins the path.
    ctx.arc(136,120,80,1.6*Math.PI,1.9*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(161,60,17,0.85*Math.PI,1.5*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(210,18,80,0.6*Math.PI,0.8*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.
        
    


    //Hair left
    ctx.beginPath();//Begins the path.
    ctx.arc(133,130,60,1.4*Math.PI,1.6*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath(); //Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(120,84,12, 0.75*Math.PI,1.4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.   
    
    ctx.beginPath();//Begins the path.
    ctx.arc(140,-4,100,0.45*Math.PI,0.6*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.stroke();//Strokes the path.
    ctx.closePath();//Closes the path.  
        
    //Hair fill    
    ctx.beginPath();//Begins the path.
    ctx.arc(162,61,15,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.       
    
    ctx.beginPath();//Begins the path.
    ctx.arc(173,69,15,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.      
        
    ctx.beginPath();//Begins the path.
    ctx.arc(180,74,15,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path. 
        
    ctx.beginPath();//Begins the path.
    ctx.arc(186,78,13,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path. 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(194,90,12,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path. 
    
    ctx.beginPath();//Begins the path.
    ctx.arc(202,96,8,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(120,83,10,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(125,83,11,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(136,83,11,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
     
    ctx.beginPath();//Begins the path.
    ctx.arc(142,83,11,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(148,81,7,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(148,87,7,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.arc(153,84,7,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(155,87,7,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(157,89,9,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(162,94,10,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath();//Closes the path.
        
    ctx.beginPath();//Begins the path.
    ctx.arc(172,100,10,2*Math.PI,4*Math.PI);//Points of the arc.
    ctx.fillStyle = ("#DC2F2F2");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 4;//Chooses the line width.
    ctx.closePath(); //Closes the path.
        
   
        
    //hair ends
        
        
       
    //Top part of beak
    ctx.beginPath()//Begins the path./
    ctx.moveTo(269, 220);//This draws the right point of the triangle.
    ctx.lineTo(215, 200);//draws the top point of the triangle.
    ctx.lineTo(190, 220);//draws the left point of the triangle.
    ctx.fillStyle = ("#FFCC00");//Chooses the colour of the path.
    ctx.fill();//Fills the path.
    ctx.lineWidth = 2;//Chooses the line width.
    ctx.strokeStyle = ("#000");//This sets the colour of the stroke.
    ctx.closePath();//Closes the path.
    ctx.stroke(); //This puts the stroke colour on.
    //Top part of beak ends
        
    //Bottom Part of beak   
    ctx.beginPath();//Begins the path.
    ctx.moveTo(220, 240);//This draws the bottom point of the triangle.
    ctx.lineTo(194, 220);//This draws the left point of the triangle.
    ctx.lineTo(255, 220);//This draws the right point of the triangle.
    ctx.fillStyle = ("#FFCC00");//Chooses the colour of the path.
    ctx.fill(); //Fills the path.
    ctx.lineWidth = 2;//Chooses the line width.
    ctx.strokeStyle = ("#000");//This sets the colour of the stroke.
    ctx.closePath();//Closes the path.
    ctx.stroke();//This puts the stroke colour on.
    
    //eyebrows x2
    ctx.beginPath();//Begins the path.
    ctx.lineWidth = 5;//Chooses the line width.
    ctx.rotate(20*Math.PI/180);//Rotates the rectangle.
    ctx.fillStyle = ("#000");//Chooses the colour of the path.
    ctx.fillRect(225, 90, 40,10);//Fills the path that it has been set to fill.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();
    ctx.lineWidth = 5;//Chooses the line width.
    ctx.rotate(45*Math.PI/180);//Rotates the rectangle.
    ctx.fillRect(245,-155, 10,40);//Fills the path that it has been set to fill.
    ctx.closePath();//Closes the path.
    
    //Eyebrows end
    
    
    //Tail feathers x3
    ctx.beginPath();//Begins the path.
    ctx.lineWidth = 5;//Chooses the line width.
    ctx.rotate(40*Math.PI/180);//Rotates the rectangle.
    ctx.fillRect(170, -130, 10,35);//Fills the path that it has been set to fill.
    ctx.closePath();//Closes the path.
    
    ctx.beginPath();//Begins the path.
    ctx.lineWidth = 5;//Chooses the line width.
    ctx.rotate(50*Math.PI/180);//Rotates the rectangle.
    ctx.fillRect(10, -212, 10,30);//Fills the path that it has been set to fill.
    ctx.closePath();//Closes the path.
        
        
    ctx.beginPath();//Begins the path.
    ctx.lineWidth = 5;//Chooses the line width.
    ctx.rotate(-20*Math.PI/180);//Rotates the rectangle.
    ctx.fillRect(87, -205, 30,10);//Fills the path that it has been set to fill.
    ctx.closePath();//Closes the path.
    
    //Tail feathers end
        
        
     
        

    
        
        
        
        
        
        
        
        
        
          
    
   